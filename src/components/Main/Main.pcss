@import "../../assets/css/variables.pcss";

.page-body {
  position: relative;
  width: 1000px;
  height: 600px;
  overflow: hidden;
  box-shadow: var(--box-shadow);
}

.page-body__image {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  filter: blur(5px);
}
